<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.marler.teammap.mapper.PlayerMapper">

    <!-- 插入球员 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO player (user_id, tm_id, jersey_name, jersey_number, position)
        VALUES (#{userId}, #{tmId}, #{jerseyName}, #{jerseyNumber}, #{position})
    </insert>

</mapper>
