<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.marler.teammap.mapper.TeamRollegeRelMapper">

    <resultMap id="BaseResultMap" type="com.marler.teammap.pojo.TeamRollegeRel">
        <id column="id" property="id"/>
        <result column="team_id" property="teamId"/>
        <result column="rank" property="rank"/>
        <result column="college_id" property="collegeId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, team_id, rank, college_id, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO team_college_rel (team_id, `rank`, college_id, create_time, update_time)
        VALUES (#{teamId}, #{rank}, #{collegeId}, #{createTime}, #{updateTime})
    </insert>

    <delete id="deleteByTeamId">
        DELETE FROM team_college_rel WHERE team_id = #{teamId}
    </delete>

    <update id="updateByTeamId">
        UPDATE team_college_rel
        <set>
            <if test="rank != null">`rank` = #{rank},</if>
            <if test="collegeId != null">college_id = #{collegeId},</if>
            update_time = NOW()
        </set>
        WHERE team_id = #{teamId}
    </update>

</mapper>
